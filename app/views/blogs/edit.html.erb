<div class="container">
  <h2>記事編集</h2>
  <%= form_with model: @blog do |form| %>
  <%= render '/shared/error_messages', object: form.object %>
    

    <div class="form-group">
      <%= form.label :title %><br />
      <%= form.text_field :title, class: "form-control"  %>
    </div>

    <div class="form-group">
      <%= form.text_field :tag_ids,value: @tag_list, class: "form-control", id:'tag_ids', placeholder: "タグをつける。複数つけるには’,’で区切ってください。　例: 循環器,救急看護"   %>
    </div>

    <div class="form-group">
      <%= form.label :content %><br />
      <p>※画像が大きく表示される場合がありますが、作成後に修正されます</p>
      <%= form.rich_text_area :content %>
    </div>

    <hr>

    <div class="form-label-group col-md-3 mx-auto brog-create">
      <%= form.submit "編集", class: "btn btn-lg btn-pink btn-block" %>
    </div>
  <% end %>
</div>

<% if @blog.errors.any? %>
  <%= javascript_pack_tag 'blogs/render' %>
<%end%>
