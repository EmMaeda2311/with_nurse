<li class="user-list">
  <div class="middle-avatar">
    <% if user.avatar.attached? %>
      <%= image_tag(user.avatar.variant(resize_to_fill:[50,50]), alt: "avatar") %>
    <% else %>
      <%= image_tag("nurse.png",alt: "avatar" ,width: "50px") %>
    <% end %>
  </div>
  <%= link_to user.username, user_path(user)%>
</div>
</li>