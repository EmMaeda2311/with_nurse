<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, remoto: true) do |f| %>
  <%= f.submit "フォロー中", class: "btn btn-pink unfollow" %>
<% end %>