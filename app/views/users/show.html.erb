<div class="container">
  <h1><%= current_user.username%></h1>
  
    <div class="col-md-8">
    <% if @user.blogs.any? %>
      <h3>あなたの看護記事 (<%= @user.blogs.count %>)</h3>
      <ol class="blog">
        <%= render @blogs %>
      </ol>
      <%= will_paginate @blogs %>
    <% end %>
  </div>
</div>
