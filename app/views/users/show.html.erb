<div class="container">
  <div class="flex">
  <h1><%= current_user.username%></h1>
  <h3>の看護記事 (<%= @user.blogs.count %>)</h3>
  </div>
  
    <div class="col-md-8">
    <% if @user.blogs.any? %>
      <ol class="blog">
        <%= render @blogs %>
      </ol>
      <%= will_paginate @blogs %>
    <% end %>
  </div>
</div>
